/** Context for mobile drawer and details sheet open/close actions. */

import { createContext } from 'react'

const MobileMenuContext = createContext(null)

export const MobileMenuProvider = MobileMenuContext.Provider
