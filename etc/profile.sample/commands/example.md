---
name: greet
description: Example command showing the profile command format.
argument-hint: [name]
---

# /greet

Example command showing the profile command format.

## Usage

```
/greet [name]
```

## Workflow

1. Greet the user by name (default: "World")
2. Print the current working directory and git branch
3. List recent commits (last 5)
